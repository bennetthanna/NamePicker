<h1>Listing prizes</h1>
 
<%= link_to 'New prize', new_prize_path %>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @prizes.each do |prize| %>
    <tr>
      <td><%= prize.title %></td>
      <td><%= prize.text %></td>
      <td><%= link_to 'Show', prize_path(prize) %></td>
    </tr>
  <% end %>
</table>